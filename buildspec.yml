version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17
      nodejs: 20

  pre_build:
    commands:
      - echo "Install frontend dependencies"
      - cd frontend
      - npm install
      - cd ..

  build:
    commands:
      - echo "Build frontend"
      - cd frontend
      - npm run build
      - cd ..
      - echo "Build backend"
      - cd bookapp
      - chmod +x gradlew
      - ./gradlew clean bootJar -x test
      - cd ..
      - echo "Verify build outputs"
      - test -f frontend/dist/index.html
      - test -f bookapp/build/libs/bookapp-0.0.1-SNAPSHOT.jar
      - jar tf bookapp/build/libs/bookapp-0.0.1-SNAPSHOT.jar | grep -i mysql-connector
      - ls -al frontend/dist
      - ls -al bookapp/build/libs
      - find frontend/dist bookapp/build/libs scripts -maxdepth 3 -type f | head -100

artifacts:
  files:
    - appspec.yml
    - scripts/**/*
    - deploy-scripts/**/*
    - frontend/dist/**/*
    - bookapp/build/libs/bookapp-0.0.1-SNAPSHOT.jar
  discard-paths: no
