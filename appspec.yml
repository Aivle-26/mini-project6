version: 0.0
os: linux
file_exists_behavior: OVERWRITE

files:
  - source: /
    destination: /usr/share/nginx/html

hooks:
  AfterInstall:
    - location: scripts/restart_nginx.sh
      timeout: 60
      runas: root
